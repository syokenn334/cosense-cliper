{
  "name": "cosense-clip",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": { "test": "node --test tests/*.test.mjs", "check": "node scripts/check.mjs" },
  "devDependencies": { "@playwright/test": "^1.56.0" }
}
